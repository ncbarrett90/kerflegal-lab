# Build log

Narrative writeups of each build phase, added as phases complete. Each entry covers what was built, how, and the decisions that shaped it, with diagrams and result screenshots where they earn their space.

The change log (gitignored) is the operational ledger of state changes. The build log is the public narrative covering the same phases.
